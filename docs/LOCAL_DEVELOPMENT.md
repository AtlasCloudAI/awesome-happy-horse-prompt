# Local Development

## Generate README

```bash
npm run generate
```

The script reads `data/prompts.json` and rebuilds `README.md`.

## Notes

- Keep prompts in English for consistency.
- Do not commit real API keys.
- Extend the dataset by appending items to `data/prompts.json`.
